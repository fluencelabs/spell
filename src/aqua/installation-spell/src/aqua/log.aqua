import Op, Debug, Peer from "@fluencelabs/aqua-lib/builtin.aqua"
import Spell from "@fluencelabs/spell/spell_service.aqua"

import SpellData from "./spell_data.aqua"
import Console from "./console.aqua"


-- func console(any: T):
func log(arg: ⊤):
    spell_id <- SpellData.spell_id()
    str <- Debug.stringify(arg)
    Spell spell_id
    timestamp <- Peer.timestamp_sec()
    Spell.store_log(str)
    par Console.print([spell_id, arg])
